# LLM Provider Configuration
# The CLI automatically detects which provider to use based on available API keys
# Priority: Claude (Anthropic) > OpenAI > None (report only)

# Claude (Anthropic) - Priority 1: Get your API key from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI - Priority 2: Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Debug mode (optional)
DEBUG=false
